# Example environment configuration for AFoodoo backend
# Copy this file to .env and adjust values as needed.

# Server port – defaults to 8080 if not set
PORT=8080

# Firebase credentials – you can either set the path to your service account JSON
# or place a file named serviceAccountKey.json at the project root.
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
